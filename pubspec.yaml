name: weather_app
description: "Weather app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  weather: ^3.1.1
  flutter_bloc: ^8.1.4
  flutter_dotenv: ^5.1.0
  get_it: ^7.6.7

  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true

  assets:
    - .env